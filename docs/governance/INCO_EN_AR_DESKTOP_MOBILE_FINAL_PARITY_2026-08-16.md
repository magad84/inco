# INCO — Final EN / AR Desktop + Mobile Parity

**Date:** 2026-08-16  
**Status:** FINAL DESIGN PARITY / CURRENT FIGMA AUTHORITY  
**Production:** NOT AUTHORIZED BY THIS RECORD

## Governing English authority

INCO Figma:

`ukiYrUNG1zs38GMS3edxJ2`

Current final EN Desktop authority:

- `2:2`

The Owner direction for the final pass was to review Arabic and Mobile against the approved English version, correct any differences, and close the work.

## Current synchronized variants

- EN Desktop: `2:2`
- EN Mobile: `2:150`
- AR Desktop RTL: `40:2`
- AR Mobile RTL: `45:2`

The Mobile and Arabic variants were synchronized to the final English content/hierarchy rather than preserved as older independent baselines.

## Changes synchronized

The parity pass includes:

- current owner-approved logistics hero image/treatment;
- current product/parent header relationship;
- current rules-first/free decision-support positioning;
- human-friendly country display values (`China (CN)`, `United Arab Emirates (AE)` and Arabic equivalents) while retaining ISO values for engine logic;
- planned transaction date in Mobile intake;
- user-facing `Confirmation required` / `يلزم التأكيد` status;
- result hierarchy placing **Missing Information** and **Recommended Next Action** ahead of secondary route/cargo/destination detail;
- Official Confirmation Protocol retained;
- concise INCO Assistant language preserving that the conversational assistant cannot override the deterministic result and receives no automatic shipment-data transfer;
- founder/domain trust band aligned with current management and supply-chain expertise positioning;
- current browser-first/privacy boundary: no required account and no intentional saved/transmitted shipment case in the public V1 flow.

## RTL rules

Arabic is a real RTL localization, not a mirrored screenshot.

Do not mirror:

- INCO logo;
- photography;
- product screenshots;
- charts;
- non-directional icons.

Directional UI may follow RTL semantics. Arabic copy must preserve the same factual/decision boundary as English.

## Responsive rules

The Mobile Figma reference is 390px. Runtime implementation must also QA narrower/wider supported Mobile widths and avoid horizontal page scrolling.

Forms stack vertically, labels remain visible, touch targets remain accessible, and result hierarchy must remain the same as Desktop.

## Product boundaries unchanged

This design parity work does not change INCO product scope:

- free public rules-first decision support;
- no registration/payment/subscription requirement in current public V1;
- no saved shipment cases/uploads/persistent customer DB in current public V1;
- deterministic engine remains authoritative;
- INCO Assistant is explanatory/subordinate;
- primary current destination packs remain UAE, Saudi Arabia, Egypt and Oman;
- official confirmation is required where current evidence/rules require it.

## Implementation authority

Codex may implement/reconcile the four variants and perform Preview/Staging responsive, RTL, accessibility, SEO and browser QA without inventing a new INCO design source.

Use the current Figma nodes above plus the latest INCO repository governance at implementation time.

## Production boundary

This record does not authorize Production deployment, destructive Production actions, DNS/cutover or Production secrets.