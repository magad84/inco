{
  "name": "@inco/domain-core",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:public": "node ../../scripts/build-public.mjs",
    "test": "npm run build && node --test dist/test/**/*.test.js",
    "check": "npm run test && npm run build:public",
    "uat:first-wave": "npm run build && node dist/src/uat-cli.js --wave first",
    "uat:first-wave:ar": "npm run build && node dist/src/uat-cli.js --wave first --ar",
    "uat:second-wave": "npm run build && node dist/src/uat-cli.js --wave second",
    "uat:second-wave:ar": "npm run build && node dist/src/uat-cli.js --wave second --ar",
    "uat:all": "npm run build && node dist/src/uat-cli.js --wave all",
    "uat:all:ar": "npm run build && node dist/src/uat-cli.js --wave all --ar",
    "uat:console": "npm run build && node dist/src/uat-server.js"
  },
  "dependencies": {
    "decimal.js": "^10.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=22"
  }
}
